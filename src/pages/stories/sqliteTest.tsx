//import { Database } from "bun:sqlite";
//const db = new Database("mydb.sqlite");

export const SqliteTest = () => {
    return (
        <>
          <h2>Sqlite test but bun:sqlite fails to install... Need more work later. </h2>
         </>
      );
}